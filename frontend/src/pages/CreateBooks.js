import React from 'react';
import Layout from '../components/Layout';

const CreateBooks = () => {
    return (
        <Layout>
            create book
        </Layout>
    );
};

export default CreateBooks;